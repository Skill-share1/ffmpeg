name: Build LGPL FFmpeg for macOS

on:
  workflow_dispatch:
    inputs:
      ffmpeg_version:
        description: "FFmpeg version tag (e.g., n7.1)"
        required: true
        default: "n7.1"
      create_release:
        description: "Create a GitHub release"
        type: boolean
        default: true
  push:
    paths:
      - ".github/workflows/build-ffmpeg-macos-lgpl.yml"
    branches:
      - main

env:
  FFMPEG_VERSION: ${{ github.event.inputs.ffmpeg_version || 'n7.1' }}

jobs:
  build:
    name: Build FFmpeg (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: macos-15-intel
          - arch: arm64
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install build dependencies
        run: |
          brew install nasm yasm pkg-config

      - name: Clone FFmpeg
        run: |
          git clone --depth 1 --branch ${{ env.FFMPEG_VERSION }} https://github.com/FFmpeg/FFmpeg.git ffmpeg-src

      - name: Configure FFmpeg (LGPL)
        working-directory: ffmpeg-src
        run: |
          ./configure \
            --prefix=${{ github.workspace }}/ffmpeg-build \
            --arch=${{ matrix.arch }} \
            --enable-shared \
            --disable-static \
            --disable-gpl \
            --disable-nonfree \
            --disable-version3 \
            --enable-videotoolbox \
            --enable-audiotoolbox \
            --enable-hwaccel=h264_videotoolbox \
            --enable-hwaccel=hevc_videotoolbox \
            --enable-hwaccel=prores_videotoolbox \
            --enable-hwaccel=vp9_videotoolbox \
            --disable-libx264 \
            --disable-libx265 \
            --disable-libvpx \
            --disable-libfdk-aac \
            --disable-doc \
            --disable-htmlpages \
            --disable-manpages \
            --disable-podpages \
            --disable-txtpages \
            --disable-programs \
            --enable-avcodec \
            --enable-avformat \
            --enable-avutil \
            --enable-swresample \
            --enable-swscale \
            --enable-postproc \
            --extra-cflags="-mmacosx-version-min=11.0" \
            --extra-ldflags="-mmacosx-version-min=11.0"

      - name: Build FFmpeg
        working-directory: ffmpeg-src
        run: |
          make -j$(sysctl -n hw.ncpu)

      - name: Install FFmpeg
        working-directory: ffmpeg-src
        run: |
          make install

      - name: Create FFmpeg.framework structure
        run: |
          VERSION=$(echo "${{ env.FFMPEG_VERSION }}" | sed 's/^n//')
          FRAMEWORK_DIR="${{ github.workspace }}/FFmpeg.framework"

          mkdir -p "$FRAMEWORK_DIR/Libraries"
          mkdir -p "$FRAMEWORK_DIR/Headers"

          cp -R ${{ github.workspace }}/ffmpeg-build/include/* "$FRAMEWORK_DIR/Headers/"

          for dylib in ${{ github.workspace }}/ffmpeg-build/lib/*.dylib; do
            if [ -L "$dylib" ]; then
              continue
            fi
            cp "$dylib" "$FRAMEWORK_DIR/Libraries/"
          done

          cat > "$FRAMEWORK_DIR/Info.plist" << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleIdentifier</key>
            <string>org.ffmpeg.FFmpeg</string>
            <key>CFBundleName</key>
            <string>FFmpeg</string>
            <key>CFBundleVersion</key>
            <string>${VERSION}</string>
            <key>CFBundlePackageType</key>
            <string>FMWK</string>
          </dict>
          </plist>
          EOF

          echo "Created FFmpeg.framework structure"
          ls -la "$FRAMEWORK_DIR"
          ls -la "$FRAMEWORK_DIR/Libraries"

      - name: Fix dylib install names for framework
        run: |
          cd ${{ github.workspace }}/FFmpeg.framework/Libraries

          for dylib in *.dylib; do
            echo "Processing $dylib..."

            install_name_tool -id "@rpath/FFmpeg.framework/Libraries/$dylib" "$dylib"

            for dep in $(otool -L "$dylib" | grep -E "@rpath/lib(av|sw|postproc)" | awk '{print $1}'); do
              dep_name=$(basename "$dep")
              install_name_tool -change "$dep" "@rpath/FFmpeg.framework/Libraries/$dep_name" "$dylib"
            done

            for dep in $(otool -L "$dylib" | grep "${{ github.workspace }}/ffmpeg-build/lib" | awk '{print $1}'); do
              dep_name=$(basename "$dep")
              install_name_tool -change "$dep" "@rpath/FFmpeg.framework/Libraries/$dep_name" "$dylib"
            done
          done

          echo "Verifying install names..."
          for dylib in *.dylib; do
            echo "=== $dylib ==="
            otool -L "$dylib" | head -5
          done

      - name: Create archive
        run: |
          cd ${{ github.workspace }}
          VERSION=$(echo "${{ env.FFMPEG_VERSION }}" | sed 's/^n//')

          mkdir -p ffmpeg-package/lib
          mkdir -p ffmpeg-package/include
          cp -R ffmpeg-build/lib/* ffmpeg-package/lib/
          cp -R ffmpeg-build/include/* ffmpeg-package/include/
          cp -R FFmpeg.framework ffmpeg-package/

          tar -cJf ffmpeg-${VERSION}-lgpl-macos-${{ matrix.arch }}.tar.xz -C ffmpeg-package .

          echo "Archive contents:"
          tar -tvf ffmpeg-${VERSION}-lgpl-macos-${{ matrix.arch }}.tar.xz | head -30

          ls -la ffmpeg-${VERSION}-lgpl-macos-${{ matrix.arch }}.tar.xz

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ffmpeg-macos-${{ matrix.arch }}
          path: ffmpeg-*-lgpl-macos-${{ matrix.arch }}.tar.xz
          retention-days: 30

  release:
    name: Create Release
    needs: build
    if: ${{ github.event.inputs.create_release == 'true' || github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts

      - name: List artifacts
        run: |
          find artifacts -type f -name "*.tar.xz"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: FFmpeg ${{ env.FFMPEG_VERSION }} LGPL macOS
          tag_name: ffmpeg-${{ env.FFMPEG_VERSION }}-lgpl-macos
          draft: false
          prerelease: false
          generate_release_notes: false
          body: |
            ## FFmpeg ${{ env.FFMPEG_VERSION }} LGPL Build for macOS

            This is an LGPL-licensed build of FFmpeg for macOS, compatible with commercial/closed-source distribution.

            ### Features
            - **License**: LGPL v2.1+ (no GPL components)
            - **Hardware Acceleration**: VideoToolbox (H.264, HEVC, ProRes, VP9)
            - **Audio**: AudioToolbox AAC encoder
            - **Architectures**: x86_64 (Intel) and arm64 (Apple Silicon)
            - **Minimum macOS**: 11.0 (Big Sur)

            ### Excluded (GPL) Components
            - libx264
            - libx265
            - libvpx
            - libfdk-aac

            ### Usage
            Download the appropriate archive for your architecture and extract to `target/native-deps/`.
          files: |
            artifacts/**/*.tar.xz
